= link_to 'Back', {:action => :index}
.resource_name Edit Job Agent
= form_for @resource do |f|
  %div
    = f.label :keywords, "Enter keywords, one per line"
    %br
    = f.text_area :keywords
  = f.submit 'Update'